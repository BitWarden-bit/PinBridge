[package]
name = "pinbridge-mcp"
version = "0.1.0"
edition = "2021"
description = "MCP stdio control plane for an already-running pinbridge-agent"

[dependencies]
pinbridge-hub-core = { path = "../pinbridge-hub-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[package]
name = "pinbridge-hub"
version = "0.1.0"
edition = "2021"
description = "Headless PinBridge Hub for trusted human and AI adapters"

[dependencies]
pinbridge-hub-core = { path = "../pinbridge-hub-core" }
serde_json = "1"

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.61.2", features = ["Win32_System_Console"] }
